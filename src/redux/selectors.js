export const selectProducts = state => state.products.products;
export const selectIsEdited = state => state.products.isEdited;
export const selectTempProduct = state => state.products.tempProduct;

export const selectFilter = state => state.filter.filter;
